const errors = {
  // 自定义
  CUSTOMIZE: 'customize',

  // 缺少必传参数
  MISSING_PARAMETER: 'missing_parameter',

  // 不支持的参数类型
  UNSUPPORTED_PARAMETER_TYPE: 'unsupported_parameter type',

  // 参数为非空的
  PARAMETER_IS_NON_EMPTY: 'parameter_is_non_empty',

  // 不合格的参数格式
  UNQUALIFIED_PARAMETER_FORMAT: 'unqualified_parameter_format'
}

module.exports = {
  errors
}
